spring:
#  datasource:
#    username: ${DB_USERNAME}
#    password: ${DB_PASSWORD}
#    url: ${DB_URL}
#  это нужно, чтобы собрать url базы данных от digital ocean
#    url: jdbc:postgresql://${DB_HOST}:${DB_POST}/${DB_Name}
#    username: ${DB_USERNAME}
#    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    properties:
      mail:
#        debug: true
        smtp:
          auth: true
          starttls:
            enable: true
#      debug: true

#logging:
#  level:
#    org:
#      springframework:
#        security: DEBUG
#        mail: DEBUG

#base:
#  url: https://${NGROK_ID}.ngrok-free.app

#dropbox:
#  api:
#    host: https://api.dropboxapi.com/2/
#    token: Bearer
